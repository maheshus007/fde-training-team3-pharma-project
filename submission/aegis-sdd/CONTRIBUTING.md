# Contributing

1. Load `../AgenticApp/09_sdd_build/BUILD_SDD.md` and one task from `TASK_INDEX.md`.
2. Put product code in `packages/`, `services/`, `apps/web/`.
3. Keep parent `submission/src` as a re-export so `python submission/scripts/test.py` still works.
4. Do not add `apps/admin`, live SoR writes, or nested git.
