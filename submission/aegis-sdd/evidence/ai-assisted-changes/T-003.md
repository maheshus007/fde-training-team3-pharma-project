# AI change record — T-003

| Field | Entry |
|---|---|
| Task ID | T-003 |
| Specs loaded | SRS_API_CONTRACTS §4–5; MODULE_LAYERING; ADR-AA-002/016 |
| Files changed | `aegis-sdd/services/integration/ports/{inference,graph}.py`; `inference_stub.py`; `graph_memory.py`; `azure_openai.py` (no SDK import); `src/_canon.py`; `src/ports/*` + `src/adapters/*` shims; `tests/test_ac_platform.py`; `tests/test_ac_graph.py` (`SUPPORTS_CQ` gate) |
| ACs proven | AC-D4 stub path: `used=false` ≤50 ms; `openai` not loaded on import; empty ingest=0; forbidden edge `RESERVED_FOR` raises ValueError. Kill-switch orchestration still T-012b. CQ tests remain skipped. |
| Inference used | false |
| Tests | `python submission/scripts/test.py` OK (80, skipped=37) |
| Out of scope | Azure/Cosmos SDK; CSV ingest (T-005); CQ queries (T-006) |
| Human review | Sitting stop after T-003 |
