# AI change record — T-017

| Field | Entry |
|---|---|
| Task ID | T-017 |
| Specs loaded | DEPLOYMENT_NOTES; REPO_EXECUTION; AA-NFR-09,16 |
| Files changed | `submission/scripts/{setup,run,evaluate,reset}.py`; `tests/test_scripts.py` (`test.py` already existed) |
| ACs proven | Filenames contain setup, run, test, evaluate, reset. Reset deletes only `evidence/{idempotency,checkpoints,audit,packs}/*.json`, not `test_results.json` or challenge `data/`. Assessment scripts need no Azure keys. |
| Inference used | false |
| Tests | `python submission/scripts/test.py` OK (90) |
| Out of scope | Docker |
| Human review | Sitting stop after T-017 |
